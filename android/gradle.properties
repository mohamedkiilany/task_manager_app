org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Removed android.newDsl and android.builtInKotlin flags to align with modern AGP/Kotlin setups
# Disable Kotlin incremental compilation to avoid incremental cache locking issues on some environments
kotlin.incremental=false
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false

# Override Flutter compile/target SDK to support newer plugin AAR requirements
flutter.compileSdkVersion=36
flutter.targetSdkVersion=36
